<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>魁!!漢飯</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
     <!-- CDN -->
     
    <script src="https://code.jquery.com/jquery-2.2.4.min.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%# アイコンの追加 %>
    <%= favicon_link_tag('favicon.ico') %>
    <%# フォント読み込み %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Serif+JP:500,900&amp;subset=japanese" rel="stylesheet">
  </head>

  <body>
    <% path = request.fullpath %>
    <% if path != "/top" && path != "/users/sign_in" && path != "/users/sign_up" %>
      <header>
        <% if user_signed_in? %>
          <div class="ore<%= "_none" if path == "/users/#{current_user.id}" || path == "/users/edit" %>">
            <%= link_to image_tag(current_user.user_img.url, :class => "header__icon ore"), "/users/#{current_user.id}" %>
          </div>
          <%= link_to image_tag('/assets/logo.svg', :class => "header__icon logo"), '/posts' %>
          <div class="newmeshi<%= "_none" if path == new_post_path %>">
            <%= link_to image_tag('/assets/plus.png', :class => "header__icon newmeshi"), new_post_path %>
          </div>
        <% end %>
      </header>
    <% end %>
    <%= yield %>
  </body>
</html>
