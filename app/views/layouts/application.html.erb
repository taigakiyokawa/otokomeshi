<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>魁!!漢飯</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
     <!-- CDN -->
    <!-- CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/cropper/1.0.0/cropper.min.css" rel="stylesheet" type="text/css" media="all"/>

    <!-- JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropper/1.0.0/cropper.min.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%# アイコンの追加 %>
    <%= favicon_link_tag('favicon.ico') %>
    <%# フォント読み込み %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Serif+JP:500,900&amp;subset=japanese" rel="stylesheet">
  </head>

  <body>
    <% path = request.fullpath %>
    <% if path != "/" %>
      <header>
        <% if user_signed_in? %>
          <%= link_to image_tag(current_user.user_img.url, :class => "btn user"), edit_user_registration_path, { :class => "my_page" } %>
          <%= link_to image_tag('/assets/logo.svg', :class => "logo"), '/posts', { :class => "logo_link" } %>
          <%= link_to image_tag('/assets/plus.png', :class => "btn new"), new_post_path, { :class => "new" } %>
        <% end %>
      </header>
      <!--
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      -->
    <% end %>
    <%= yield %>
  </body>
</html>
