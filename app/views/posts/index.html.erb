
<!--
<h1>新着</h1>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><h2><%= post.title %></h2></td>
      </tr>
      <tr>
        <td><%= post.created_at.strftime('%Y/%m/%d %H:%M') %></td>
        <td><%= link_to '編集', edit_post_path(post) %></td>
        <td><%= link_to '削除', post, method: :delete, data: { confirm: '消してよいか?' } %></td>
      </tr>
      <tr>
        <td><%= link_to image_tag(post.meshi.url), post %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
 -->
<div class="container">
      <div class="tab_wrap">
      	<input id="tab1" type="radio" name="tab_btn" checked>
      	<input id="tab2" type="radio" name="tab_btn">
      	<input id="tab3" type="radio" name="tab_btn">

        <div class="tab_area">
      		<label class="tab1_label" for="tab1">新着</label>
      		<label class="tab2_label" for="tab2">番付</label>
      		<label class="tab3_label" for="tab3">嗜好</label>
    	  </div>
      	<div class="panel_area">
          <div id="panel1" class="tab_panel">
            <% @posts.each do |post| %>
              <h2><%= post.title %></h2>
              <!-- 
              <%= post.created_at.strftime('%Y/%m/%d %H:%M') %>
              <%= link_to '編集', edit_post_path(post) %>
              <%= link_to '削除', post, method: :delete, data: { confirm: '消してよいか?' } %>
              -->
              <%= link_to image_tag(post.meshi.url), post %>
            <% end %>
      		</div>
      		<div id="panel2" class="tab_panel">

      		</div>
      		<div id="panel3" class="tab_panel">

      		</div>
        </div>
      </div>
    </div>